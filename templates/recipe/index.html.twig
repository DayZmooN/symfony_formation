{% extends 'base.html.twig' %}

{% block title %}Toute les recette{% endblock %}

{% block body %}


    <div class="container my-4">
        <h1 class="mb-4">Mes Recettes</h1>
        <a href="{{ path('recette.create') }}" class="btn btn-primary mb-3">Créer une Recette</a>

        <table class="table table-striped">
            <thead>
            <tr>
                <th scope="col">Titre</th>
                <th scope="col">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for recipe in recipes %}
                <tr>
                    <td>
                        <a href="{{ path('recette.show', {id: recipe.id, slug: recipe.slug}) }}" class="text-decoration-none">{{ recipe.title }}</a>
                    </td>
                    <td>
                        <div class="d-flex gap-1">
                            <a href="{{ path('recette.edit', {id: recipe.id}) }}" class="btn btn-sm btn-secondary">Modifier</a>
                          {#  <form action="{{ path('recette.delete',{id:recipe.id}) }}" method="post">
                                <input type="hidden" value="DELETE" name="_method">
                            <a type="submit" href="{{ path('recette.delete',{id:recipe.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>

                            </form>
                          #}
                            <form method="post">
                            <a type="submit"  href="{{ path('recette.delete',{id:recipe.id}) }}" class="btn btn-sm btn-danger">Supprimer</a>
                            </form>
                        </div>

                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="2" class="text-center">Aucune recette disponible.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>



{% endblock %}
